# 第 02 章 · 思考题与面试题

## 思考题

1. CoT 为什么有效？给出至少两种解释（自回归暴露中间结果 / mode-shift 等）。
2. 如果 LLM 已经被 RL 训练成「思考 → 答」的模式（如 DeepSeek-R1），那 ReAct 还有必要吗？
3. ToT 和 A* 搜索的相同点与不同点是什么？
4. 写出 ReAct 在哪些情况下会陷入死循环，并给出至少 3 种工程对策。

## 面试题

1. **(基础)** 把 ReAct prompt 用伪代码写出来。
2. **(深入)** 解释 LATS 中 UCT 的物理含义；为什么需要 $c$ 这个超参？
3. **(系统设计)** 你要给一个写作助手加「迭代修改」能力，应该用 Self-Refine 还是 Reflexion？为什么？
4. **(开放)** 在路径规划 / SLAM 这类传统几何任务里，能否找到一个步骤适合用「思路 + 评估」的搜索范式？请举例并讨论代价。
